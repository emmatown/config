{
  # This is only the initial selection; the broker accepts any model requested
  # by the Codex client and the account's upstream model availability applies.
  model = "gpt-5.6-sol";
  concurrency = 2;
}
